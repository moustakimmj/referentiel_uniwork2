server:
  port: 8082

spring:
  application :
    name : ai-agent-service

  datasource:
    url: jdbc:postgresql://localhost:5432/aidb
    username: postgres
    password: postgres

logging:
  level:
    root: INFO
    org.springframework.web: DEBUG
